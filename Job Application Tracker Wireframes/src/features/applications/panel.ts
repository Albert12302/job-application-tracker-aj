/** The centred card the add, edit, and detail screens sit in (§4.3, §4.4, §11). */
export const PANEL =
  'mx-auto flex w-full max-w-[460px] flex-col gap-4 rounded-xl border bg-card p-6 shadow-xs max-[760px]:p-4';

/** The small label above each block of the detail screen. */
export const SECTION_HEADING = 'text-[13px] font-semibold text-muted-foreground';
